# DatabaseManagerPro

<div align="center">

**作者：LYP** | [GitHub](https://github.com/lyp0746) | [邮箱](mailto:1610369302@qq.com)

![Version](https://img.shields.io/badge/version-2.0.0-blue.svg)
![Python](https://img.shields.io/badge/python-3.7+-green.svg)
![PyQt5](https://img.shields.io/badge/PyQt5-5.15+-orange.svg)
![License](https://img.shields.io/badge/license-MIT-brightgreen.svg)

**专业的跨平台数据库管理工具**

[功能特性](#功能特性) • [安装说明](#安装说明) • [使用指南](#使用指南) • [快捷键](#快捷键) • [截图展示](#截图展示)

</div>

---

## 📖 简介

DatabaseManagerPro 是一款基于 Python 和 PyQt5 开发的专业数据库管理工具，提供直观的图形界面和强大的功能，支持多种数据库系统。无论您是数据库管理员、开发人员还是数据分析师，都能通过这款工具高效地管理和操作数据库。

## ✨ 功能特性

### 核心功能

- 🔌 **多数据库支持**
  - SQLite（内置支持，无需额外配置）
  - MySQL（通过 pymysql）
  - PostgreSQL（通过 psycopg2）

- 📝 **高级SQL编辑器**
  - 语法高亮显示
  - 行号显示
  - 当前行高亮
  - 自动缩进
  - 代码注释/取消注释（Ctrl+/）
  - SQL格式化（Ctrl+Shift+F）
  - 选中执行或全部执行

- 📊 **数据管理**
  - 表数据查看与浏览
  - 表结构查看
  - 数据导入（CSV、JSON）
  - 数据导出（CSV、JSON、Excel）
  - 批量数据操作
  - 结果集复制

- 🔍 **查询功能**
  - 快速查询执行（F5 / Ctrl+Enter）
  - 查询历史记录
  - 历史搜索过滤
  - 一键加载历史SQL
  - 查询性能分析（EXPLAIN）

- 🛠️ **数据库工具**
  - 表管理（创建、删除、清空）
  - 数据库备份（SQLite）
  - 数据库恢复
  - 性能优化建议
  - 性能监控
  - 数据库大小统计

- 🎨 **用户界面**
  - 现代化扁平设计
  - 响应式布局
  - 多标签页支持
  - 可定制的窗口布局
  - 实时状态显示
  - 右键上下文菜单

## 📦 安装说明

### 系统要求

- Python 3.7 或更高版本
- 操作系统：Windows / Linux / macOS

### 基础安装

```bash
# 1. 克隆或下载项目
git clone https://github.com/yourusername/DatabaseManagerPro.git
cd DatabaseManagerPro

# 2. 安装基础依赖（必需）
pip install PyQt5
```

### 可选依赖

根据需要安装以下可选依赖：

```bash
# MySQL 支持
pip install pymysql

# PostgreSQL 支持
pip install psycopg2-binary

# Excel 导出支持
pip install pandas openpyxl
```

### 一键安装所有依赖

```bash
# 安装所有依赖
pip install PyQt5 pymysql psycopg2-binary pandas openpyxl
```

## 🚀 快速开始

### 启动程序

```bash
python DatabaseManagerPro.py
```

### 首次使用

1. **连接数据库**
   - 点击工具栏的"🔌 连接"按钮
   - 选择数据库类型（SQLite/MySQL/PostgreSQL）
   - 填写连接参数
   - 点击"确定"

2. **执行查询**
   - 在SQL编辑器中输入SQL语句
   - 按 `F5` 或点击"▶ 执行"按钮
   - 查看结果表格

3. **浏览表数据**
   - 在左侧表列表中双击表名
   - 自动生成并执行 SELECT 查询
   - 查看表数据和统计信息

## 📘 使用指南

### 连接数据库

#### SQLite 数据库

```
1. 点击"连接"按钮
2. 选择"SQLite"
3. 输入连接名称（可选）
4. 点击"浏览"选择.db文件
   - 选择现有文件打开数据库
   - 输入新文件名创建新数据库
5. 点击"确定"
```

#### MySQL 数据库

```
1. 点击"连接"按钮
2. 选择"MySQL"
3. 填写连接信息：
   - 主机：localhost（或远程IP）
   - 端口：3306（默认）
   - 用户名：root
   - 密码：your_password
   - 数据库：database_name
4. 点击"确定"
```

#### PostgreSQL 数据库

```
1. 点击"连接"按钮
2. 选择"PostgreSQL"
3. 填写连接信息：
   - 主机：localhost
   - 端口：5432（默认）
   - 用户名：postgres
   - 密码：your_password
   - 数据库：database_name
4. 点击"确定"
```

### SQL 编辑器

#### 基本操作

- **编写SQL**：直接在编辑器中输入
- **执行查询**：F5 或 Ctrl+Enter
- **选中执行**：选中部分SQL后按F5
- **格式化**：Ctrl+Shift+F
- **注释**：Ctrl+/（切换注释）
- **保存SQL**：Ctrl+S
- **打开SQL文件**：Ctrl+O

#### 语法高亮

编辑器自动高亮以下内容：
- **蓝色**：SQL关键字（SELECT、FROM、WHERE等）
- **紫色**：函数名（COUNT、SUM、MAX等）
- **绿色**：字符串（'text'、"text"）
- **橙色**：数字
- **灰色**：注释（-- 注释）

### 表管理

#### 查看表数据

```
方法1：双击表名
方法2：右键表名 -> 查看数据
```

#### 查看表结构

```
右键表名 -> 查看结构
显示列名、数据类型、约束等信息
```

#### 导出表

```
右键表名 -> 导出表
选择格式：CSV / JSON / Excel
```

#### 清空表

```
右键表名 -> 清空表
删除所有数据，保留表结构
```

#### 删除表

```
右键表名 -> 删除表
⚠️ 此操作不可恢复！
```

### 数据导入导出

#### 导入数据

```
1. 文件 -> 导入数据
2. 选择CSV或JSON文件
3. 输入目标表名
4. 选择选项：
   ✓ 如果表不存在则创建
   ✓ 导入前清空表
5. 点击"开始导入"
```

#### 导出数据

```
方法1：导出查询结果
  - 执行查询后
  - 点击"📊 导出CSV/JSON/Excel"

方法2：导出整表
  - 文件 -> 导出数据
  - 选择表和格式
  - 选择保存位置
```

### 查询历史

#### 查看历史

```
1. 切换到"查询历史"标签
2. 浏览执行过的SQL语句
3. 双击历史项加载到编辑器
```

#### 搜索历史

```
在历史标签页顶部输入框输入关键字
自动过滤匹配的历史记录
```

#### 清空历史

```
点击"清空历史"按钮
历史记录会自动保存，重启后恢复
```

### 性能优化

#### 查询分析

```
1. 编写查询语句
2. 查询 -> 查询分析
3. 查看执行计划和性能信息
```

#### 数据库优化

```
1. 工具 -> 性能优化
2. 查看优化建议
3. 点击"执行优化"
   - SQLite: 执行 VACUUM 和 ANALYZE
   - 其他：查看建议手动优化
```

#### 性能监控

```
1. 工具 -> 性能监控
2. 查看数据库统计信息：
   - 数据库大小
   - 表数量
   - 各表行数
3. 点击"刷新"更新信息
```

## ⌨️ 快捷键

### 文件操作

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+N` | 新建连接 |
| `Ctrl+O` | 打开SQL文件 |
| `Ctrl+S` | 保存SQL文件 |
| `Ctrl+Q` | 退出程序 |

### 编辑操作

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+/` | 注释/取消注释 |
| `Ctrl+Shift+F` | 格式化SQL |
| `Ctrl+A` | 全选 |
| `Ctrl+C` | 复制 |
| `Ctrl+V` | 粘贴 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` | 重做 |

### 查询操作

| 快捷键 | 功能 |
|--------|------|
| `F5` | 执行查询 |
| `Ctrl+Enter` | 执行查询 |
| `Ctrl+E` | 执行选中的SQL |

### 其他

| 快捷键 | 功能 |
|--------|------|
| `F1` | 帮助文档 |
| `Ctrl+F` | 搜索（历史） |

## 📸 截图展示

### 主界面
```
┌─────────────────────────────────────────────────────────────┐
│  DatabaseManagerPro - 专业数据库管理工具                    │
├─────────────────────────────────────────────────────────────┤
│ 🔌 连接  ▶ 执行  📋 格式化  🗑 清空  🔄 刷新              │
├──────────────┬──────────────────────────────────────────────┤
│              │  SQL编辑器                                   │
│  表列表      │  ┌──────────────────────────────────────┐   │
│  ├─ users   │  │ 1 SELECT * FROM users                │   │
│  ├─ orders  │  │ 2 WHERE status = 'active'            │   │
│  └─ products│  │ 3 ORDER BY created_at DESC           │   │
│              │  └──────────────────────────────────────┘   │
│  行数统计    │  ──────────────────────────────────────    │
│  users: 1250│  查询结果                                    │
│  orders: 530│  ┌──────────────────────────────────────┐   │
│              │  │ id │ name    │ email    │ status   │   │
│              │  ├────┼─────────┼──────────┼──────────┤   │
│              │  │ 1  │ Alice   │ a@...    │ active   │   │
│              │  │ 2  │ Bob     │ b@...    │ active   │   │
│              │  └──────────────────────────────────────┘   │
└──────────────┴──────────────────────────────────────────────┘
│ 已连接到 SQLite | 查询完成，共 2 行，耗时: 0.003秒       │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 高级功能

### 批量数据操作

```sql
-- 批量插入
INSERT INTO users (name, email) VALUES
('User1', 'user1@example.com'),
('User2', 'user2@example.com'),
('User3', 'user3@example.com');

-- 批量更新
UPDATE users 
SET status = 'inactive' 
WHERE last_login < '2023-01-01';

-- 批量删除
DELETE FROM logs 
WHERE created_at < DATE('now', '-30 days');
```

### 复杂查询示例

```sql
-- 联表查询
SELECT u.name, COUNT(o.id) as order_count
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
GROUP BY u.id
HAVING order_count > 5
ORDER BY order_count DESC;

-- 子查询
SELECT * FROM products
WHERE category_id IN (
    SELECT id FROM categories 
    WHERE status = 'active'
);

-- 窗口函数（支持的数据库）
SELECT 
    name,
    salary,
    RANK() OVER (ORDER BY salary DESC) as rank
FROM employees;
```

### 数据分析

```sql
-- 统计分析
SELECT 
    DATE(created_at) as date,
    COUNT(*) as total_orders,
    SUM(amount) as total_amount,
    AVG(amount) as avg_amount
FROM orders
WHERE created_at >= DATE('now', '-7 days')
GROUP BY DATE(created_at)
ORDER BY date;

-- 数据去重
SELECT DISTINCT category, brand 
FROM products
ORDER BY category, brand;
```

## 📂 项目结构

```
DatabaseManagerPro/
│
├── DatabaseManagerPro.py      # 主程序文件
├── README.md                   # 说明文档
├── requirements.txt            # 依赖列表
├── LICENSE                     # 许可证
│
├── assets/                     # 资源文件（可选）
│   ├── icons/                  # 图标
│   └── screenshots/            # 截图
│
└── examples/                   # 示例文件（可选）
    ├── sample.db               # 示例SQLite数据库
    ├── sample_data.csv         # 示例CSV数据
    └── sample_queries.sql      # 示例SQL查询
```

## 🐛 常见问题

### 1. 无法连接MySQL数据库

**问题**：提示"请安装 pymysql"

**解决**：
```bash
pip install pymysql
```

### 2. 无法导出Excel文件

**问题**：提示"请安装 pandas 和 openpyxl"

**解决**：
```bash
pip install pandas openpyxl
```

### 3. 中文显示乱码

**问题**：CSV导出后中文乱码

**解决**：
- 程序已使用 utf-8-sig 编码
- 使用Excel打开时选择UTF-8编码
- 或使用专业CSV编辑器（如 VSCode）

### 4. SQLite数据库锁定

**问题**：提示"database is locked"

**解决**：
- 关闭其他正在访问该数据库的程序
- 确保没有未提交的事务
- 重新连接数据库

### 5. 查询执行缓慢

**解决**：
- 使用 LIMIT 限制返回行数
- 为常用字段添加索引
- 使用 EXPLAIN 分析查询计划
- 使用"性能优化"功能

## 🔒 安全建议

1. **密码安全**
   - 不要在代码中硬编码密码
   - 程序不会保存数据库密码
   - 每次连接都需要重新输入

2. **权限管理**
   - 使用最小权限原则
   - 生产环境避免使用root账户
   - 定期审查数据库权限

3. **数据备份**
   - 定期备份重要数据库
   - 测试备份恢复流程
   - 将备份存储在安全位置

4. **SQL注入防护**
   - 使用参数化查询
   - 验证用户输入
   - 限制执行权限

## 🛣️ 路线图

### v2.1.0（计划中）
- [ ] 数据库连接池管理
- [ ] 支持更多数据库（Oracle、SQL Server）
- [ ] SQL代码补全
- [ ] 查询结果可视化（图表）
- [ ] 主题切换（亮色/暗色）

### v2.2.0（计划中）
- [ ] 数据库对比工具
- [ ] 结构同步功能
- [ ] 数据迁移向导
- [ ] 批量SQL执行
- [ ] 查询计划可视化

### v3.0.0（远期）
- [ ] 插件系统
- [ ] 云数据库支持
- [ ] 团队协作功能
- [ ] 在线SQL分享
- [ ] 移动端支持

## 🤝 贡献指南

欢迎提交问题和贡献代码！

### 如何贡献

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码规范

- 遵循 PEP 8 编码规范
- 添加必要的注释和文档字符串
- 编写单元测试
- 更新 README 文档

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 👨‍💻 作者

DatabaseManagerPro Team

## 📮 联系方式

- 📧 Email: your.email@example.com
- 🐛 Issues: [GitHub Issues](https://github.com/yourusername/DatabaseManagerPro/issues)
- 💬 Discussions: [GitHub Discussions](https://github.com/yourusername/DatabaseManagerPro/discussions)

## 🙏 致谢

感谢以下开源项目：

- [PyQt5](https://www.riverbankcomputing.com/software/pyqt/) - GUI框架
- [pymysql](https://github.com/PyMySQL/PyMySQL) - MySQL驱动
- [psycopg2](https://www.psycopg.org/) - PostgreSQL驱动
- [pandas](https://pandas.pydata.org/) - 数据处理

## ⭐ Star History

如果这个项目对您有帮助，请给它一个 Star ⭐

---

<div align="center">

**[⬆ 回到顶部](#databasemanagerpro)**

Made with ❤️ by DatabaseManagerPro Team

</div>